import type { RoadmapItem } from "~/models/RoadmapItem";

const ItemTimelog = ({
  item,
  timelog,
}: {
  item: RoadmapItem;
  timelog: any;
}) => {
  return <>Hello, World!</>;
};

export default ItemTimelog;
